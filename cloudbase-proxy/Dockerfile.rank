FROM node:22-bookworm
WORKDIR /app
COPY rank-package.json package.json
RUN npm install --omit=dev && npx playwright install --with-deps chromium
COPY rank-service.js ./
ENV NODE_ENV=production
ENV PORT=3000
EXPOSE 3000
CMD ["node","rank-service.js"]